<tr class="<?='movie'.htmlspecialchars($movie['idMovie']) ?>">
    <td>
        <p id="<?="movie".htmlspecialchars($movie['idMovie']) ?>"><?= htmlspecialchars($movie['title']) ?></p>
    </td>
    <td>
        <p><?= htmlspecialchars($movie['summary']) ?></p>
    </td>
    <td>
        <p><?= htmlspecialchars($movie['poster']) ?></p>
    </td>
    <td>
        <p><?= htmlspecialchars($movie['releaseYear']) ?></p>
    </td>
    <td>
        <p><?= htmlspecialchars($movie['userRating']) ?></p>
    </td>
    <td>
        <p><?= htmlspecialchars($movie['createdAt']) ?></p>
    </td>
    <td>
        <p><?= htmlspecialchars($movie['userId']) ?></p>
    </td>
    <td>
        <button data-idMovie="<?=htmlspecialchars($movie['idMovie']) ?>"
                data-ajax="<?=htmlspecialchars(buildUrl('delete_movie', ['idMovie' => $movie['idMovie'] ]));?>"
                class="deleteMovie-btn"
                type="button">
            delete
        </button>
        <button data-idMovie="<?=htmlspecialchars($movie['idMovie']) ?>"
                data-ajax="<?=htmlspecialchars(buildUrl('edit_movie', ['idMovie' => $movie['idMovie']]));?>"
                class="editMovie-btn" type="button">
            edit movie
        </button>

    </td>
</tr>
